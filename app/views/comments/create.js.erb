<% if @comment.valid? %>
  $("#comments").append("<%= escape_javascript(render @comment) %>");
  $("#<%= dom_id(@comment) %>").show("clip");
  <% @comment = Comment.new %>
<% end %>

$("#new_comment").html("<%= escape_javascript(render("form", post: @post, comment: @comment)) %>");