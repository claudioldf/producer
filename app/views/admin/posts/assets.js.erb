$("#media").append("<%= escape_javascript(render partial: "asset", collection: @assets) %>");

<% if @assets.total_pages == @assets.current_page %>
  $("#more-assets").hide();
<% else %>
  $("#more-assets").attr("href", "<%= assets_admin_posts_path(page: @assets.next_page) %>");
<% end %>
